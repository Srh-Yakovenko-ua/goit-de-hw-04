### Домашнє завдання до теми «Apache Spark. Оптимізація та SparkUІ»

'''

Привіт 😉

У цьому домашньому завданні код вже написаний за вас! Але це, можливо, зробить ДЗ тільки складнішим! Ваше завдання полягає у виконанні декількох варіантів одного/схожого коду і міркуванні над виглядом SparkUI.

Вам потрібно:

запустити три програми;
зробити скриншоти трьох наборів Jobs;
проаналізувати та вміти обґрунтувати наявність певної кількості Jobs у кожному наборі;
зрозуміти, що робить функція cache та навіщо її використовувати.

Посилання на необхідні дані.

Покрокова інструкція виконання
'''

### Ввести в консоль WSL наступні команди:

'''
alexdtsc@DESKTOP-31P3B89:~/goit_data_engineering_spark$ source venv/bin/activate
(venv) alexdtsc@DESKTOP-31P3B89:~/goit_data_engineering_spark$ cd ./lecture-4/de-hw-04
(venv) alexdtsc@DESKTOP-31P3B89:~/goit_data_engineering_spark/lecture-4/de-hw-04$ python3 hw-04.py
'''


## Частина 1

# # За основу візьмемо вже знайомий вам код і додамо проміжну дію:

# from pyspark.sql import SparkSession

# # Створюємо сесію Spark
# spark = SparkSession.builder \
#     .master("local[*]") \
#     .config("spark.sql.shuffle.partitions", "2") \
#     .appName("MyGoitSparkSandbox") \
#     .getOrCreate()

# # Завантажуємо датасет
# nuek_df = spark.read \
#     .option("header", "true") \
#     .option("inferSchema", "true") \
#     .csv('./nuek-vuh3.csv')

# nuek_repart = nuek_df.repartition(2)

# nuek_processed = nuek_repart \
#     .where("final_priority < 3") \
#     .select("unit_id", "final_priority") \
#     .groupBy("unit_id") \
#     .count()

# # Ось ТУТ додано рядок
# nuek_processed = nuek_processed.where("count>2")

# nuek_processed.collect()

# input("Press Enter to continue...5")

# # Закриваємо сесію Spark
# spark.stop()

'''
Запустіть код. Зробіть скриншот усіх Jobs (їх має бути 5).
'''

# Пояснення чому саме 5 Jobs

'''

Головне правило Spark: **Job (завдання) створюється лише тоді, коли викликається Action (дія)**. На перший погляд, у нашому коді лише один Action — це `nuek_processed.collect()` у самому кінці. 

Тож звідки взялися цілих 5 Jobs? Розберемо їх по черзі.

### 1. Звідки взялися Job 0 та Job 1? (Приховані Actions)
На нашому скриншоті перші два завдання мають опис, що починається з `csv at...`. 
Вони з'явилися виключно через один рядок у твоєму коді:
**`.option("inferSchema", "true")`**

Коли просимо Spark автоматично вгадати типи даних (де текст, а де числа), він більше не може бути "лінивим". 
Йому доводиться фізично відкрити CSV-файл, прочитати заголовки та просканувати частину рядків, щоб зрозуміти структуру. 
Це сканування і генерує перші два міні-завдання (Job 0 та Job 1) ще на етапі завантаження DataFrame, задовго до виклику `collect()`.

### 2. Звідки взялися Job 2, 3 та 4? (Один collect() = Три Jobs)
Усі ці три завдання (як видно з їхнього опису `collect at...`) були згенеровані нашим останнім рядком `nuek_processed.collect()`.

Чому Spark розбив одну дію на три окремі завдання? Причина ховається у двох речах: **Shuffle** (перемішування) та механізмі **AQE (Adaptive Query Execution)**, який увімкнений у Spark за замовчуванням.

AQE — це "розумний" оптимізатор. Кожного разу, коли він бачить, що дані потрібно глобально перемішати між ядрами процесора (Shuffle), 
він призупиняє основний потік, виконує перемішування як окремий Job, збирає статистику (щоб зрозуміти, скільки даних вийшло), і лише потім оптимально планує наступний крок.

У нашому коді є рівно **два місця**, які викликають жорсткий Shuffle:
1.  **`.repartition(2)`** — ми примусово сказали Spark'у перетасувати всі дані на 2 кошики.
2.  **`.groupBy("unit_id")`** — щоб згрупувати виклики, Spark знову має перекидати дані між ядрами, шукаючи однакові ID.

Через ці два "розриви" в ланцюжку, алгоритм виконання розбився на три етапи:
* **Job 2:** Читає дані та виконує роботу до першого перемішування (`repartition`).
* **Job 3:** Підхоплює дані після репартиції, фільтрує їх (`where`) і готує до групування (`groupBy`).
* **Job 4:** Завершує групування, рахує кількість (`count`), фільтрує фінальний результат (`count>2`) і видає його нам на екран.

---

**Підсумок:** 5 Jobs = 2 приховані завдання від `inferSchema` + 3 завдання від `collect()`, який розбився на частини через наявність двох операцій перемішування (`repartition` та `groupBy`) під дією оптимізатора AQE.

Ось так Spark UI допомагає бачити реальну фізичну роботу, а не лише написаний текст коду! 
'''


## Частина 2

# # Додамо проміжний Action — collect :

# from pyspark.sql import SparkSession

# # Створюємо сесію Spark
# spark = SparkSession.builder \
#     .master("local[*]") \
#     .config("spark.sql.shuffle.partitions", "2") \
#     .appName("MyGoitSparkSandbox") \
#     .getOrCreate()

# # Завантажуємо датасет
# nuek_df = spark.read \
#     .option("header", "true") \
#     .option("inferSchema", "true") \
#     .csv('./nuek-vuh3.csv')

# nuek_repart = nuek_df.repartition(2)

# nuek_processed = nuek_repart \
#     .where("final_priority < 3") \
#     .select("unit_id", "final_priority") \
#     .groupBy("unit_id") \
#     .count()
    
# # Проміжний action: collect
# nuek_processed.collect()

# # Ось ТУТ додано рядок
# nuek_processed = nuek_processed.where("count>2")

# nuek_processed.collect()

# input("Press Enter to continue...5")

# # Закриваємо сесію Spark
# spark.stop()


'''
Запустіть код. Зробіть скриншот усіх Jobs (їх має бути 8).

 🧠 Подумайте, чому при додаванні однієї проміжної дії nuek_processed.collect(), отримано аж на 3 Job більше?
'''


# Відповідь чому з 5 стало 8 Jobs? - чому при додаванні однієї проміжної дії nuek_processed.collect(), отримано аж на 3 Job більше?

'''
### Головна причина: "Ліниві обчислення" та амнезія Spark

Відповідь ховається в тому, як саме Spark ставиться до даних. За замовчуванням він **не запам'ятовує** проміжні результати. 

Ось як фізично розподілилися ці 8 завдань під час виконання твого коду:

1.  **Jobs 0 та 1:** Це етап читання та визначення типів колонок (`inferSchema`). Spark прочитав верхівку файлу, щоб зрозуміти, де текст, а де числа.

2.  **Jobs 2, 3 та 4:** Це відпрацював твій **перший (проміжний)** `nuek_processed.collect()`. 
Spark пішов на жорсткий диск, повністю прочитав файл, зробив `repartition` (розбив дані на 2 частини), згрупував їх через `groupBy` і видав тобі кількість. 
Оскільки тут було перемішування даних (Shuffle), оптимізатор розбив цю дію на 3 етапи. **Але щойно Spark видав результат, він одразу забув усе, що щойно порахував!**

3.  **Jobs 5, 6 та 7:** Це відпрацював твій **другий (фінальний)** `nuek_processed.collect()` (після застосування фільтра `where("count>2")`). 
Оскільки Spark нічого не зберіг у пам'яті після першого `collect()`, він змушений **робити всю важку роботу з самісінького початку**. 
Він знову йде на диск, знову читає весь файл, знову розбиває на партиції, знову групує, і лише потім відфільтровує потрібні рядки. 
Цей повторний прохід усього графа обчислень і згенерував рівно 3 додаткові Jobs.

---

**Висновок:**

> *"При додаванні проміжної дії `collect()` кількість Job збільшилася на 3, оскільки DataFrame не був закешований. 
Spark використовує концепцію лінивих обчислень (lazy evaluation) і не зберігає проміжні результати за замовчуванням. 
Тому кожен новий виклик Action (у нашому випадку другий `collect()`) змушує Spark заново перераховувати весь граф обчислень (DAG) з нуля: 
повторно читати файл із диска та виконувати операції перемішування (Shuffle). 
Саме цей повторний цикл обчислень і згенерував 3 додаткові Job."*

Це механіка роботи Spark без пам'яті (без кеша)
'''


## Частина 3

# Використаємо нову для вас функцію cache в проміжному результаті.

'''
 ☝🏻Функція cache() в PySpark використовується для кешування (зберігання в пам'яті) даних RDD (Resilient Distributed Dataset) або DataFrame. Це дозволяє прискорити виконання подальших дій (actions) або перетворень (transformations), які працюють з тими ж даними. Кешування особливо корисне, коли ви виконуєте декілька операцій на одному й тому ж RDD або DataFrame, оскільки PySpark не буде повторно обчислювати ті самі дані.

Як працює cache() :

1. Кешування в пам'яті. Коли ви викликаєте cache() на RDD або DataFrame, дані зберігаються в пам'яті (RAM) у розподіленому вигляді на всіх вузлах кластера. Це дозволяє прискорити подальші обчислення, оскільки Spark не буде знову завантажувати або обчислювати ці дані.
2. Ліниве виконання. Виклик cache() не призводить до негайного виконання обчислень. Лише коли ви виконуєте дію (action), наприклад, count(), collect(), або show(), дані будуть обчислені та кешовані.
3. Механізм зберігання. За замовчуванням, cache() використовує пам'ять (Memory). Однак, якщо дані не поміщаються в пам'ять, Spark буде зберігати їх на диску.
4. Контроль над кешуванням. Коли ви використовуєте cache(), Spark зберігає дані з рівнем зберігання MEMORY_ONLY. Якщо ви хочете використовувати інші рівні зберігання, такі як MEMORY_AND_DISK, можна використовувати метод persist().

☝🏻Але в такі деталі ми занурюватися на практиці не будемо. Важливо знати, що можливо зберігати як в пам’яті, так і на диску. Зберігання в пам’яті набагато більш розповсюджене, на диску — екзотика 😉
'''

from pyspark.sql import SparkSession

# Створюємо сесію Spark
spark = SparkSession.builder \
    .master("local[*]") \
    .config("spark.sql.shuffle.partitions", "2") \
    .appName("MyGoitSparkSandbox") \
    .getOrCreate()

# Завантажуємо датасет
nuek_df = spark.read \
    .option("header", "true") \
    .option("inferSchema", "true") \
    .csv('./nuek-vuh3.csv')

nuek_repart = nuek_df.repartition(2)

nuek_processed_cached = nuek_repart \
    .where("final_priority < 3") \
    .select("unit_id", "final_priority") \
    .groupBy("unit_id") \
    .count() \
    .cache()  # Додано функцію cache

# Проміжний action: collect
nuek_processed_cached.collect()

# Ось ТУТ додано рядок
nuek_processed = nuek_processed_cached.where("count>2")

nuek_processed.collect()

input("Press Enter to continue...5")

# Звільняємо пям'ять від Dataframe
nuek_processed_cached.unpersist()

# Закриваємо сесію Spark
spark.stop()


'''
Запустіть код. Зробіть скриншот усіх Jobs (їх має бути 7).

 🧠Подумайте, чому при використанні cache() ми зменшили кількість Job?
'''

# Відповідь на питання чому при використанні cache() ми зменшили кількість Job з 8 до 7?


'''

### Магія кешування: як ми вилікували "амнезію" Spark

У Частині 2 ми з'ясували, що Spark за замовчуванням забуває проміжні результати. 
Тому на другий `collect()` він витратив аж 3 Jobs, щоб перерахувати все з нуля (зчитати файл, перемішати дані, згрупувати).

Додавши `.cache()`, ми дали Spark'у вказівку: *"Коли порахуєш цю таблицю, не викидай її! Збережи її в оперативній пам'яті (RAM)"*.

Ось що фізично сталося у твоєму коді і відобразилося на екрані:

1. **Jobs 0 та 1:** Незмінні перші кроки — читання заголовків та визначення типів даних (`inferSchema`).

2. **Jobs 2, 3, 4 та 5 (Рядок 237):** Це відпрацював твій **перший** `collect()`. Spark пішов на жорсткий диск, виконав усі важкі операції з перемішуванням (Shuffle) для групування даних. 
Тут завдань стало 4 (а не 3, як раніше), тому що Spark виконав додаткову фізичну роботу — він акуратно **записав готовий результат у свою оперативну пам'ять** (матеріалізував кеш).

3. **Job 6 (Рядок 242):** А ось тут відбувається справжня магія! Це наш **другий** `collect()`. Звернемо увагу: для нього знадобився **всього 1 Job** (замість трьох)! 
    * Чому? Тому що Spark подивився на свій план і зрозумів: *"Стоп, мені не треба йти на повільний жорсткий диск і робити важкий Shuffle! 
       У мене вже є готова згрупована таблиця в швидкій оперативній пам'яті!"*. 
    * Він просто миттєво взяв цю готову таблицю з пам'яті, застосував до неї простенький фільтр `where("count>2")` і видав нам результат. 
       Оскільки не було перемішування даних між ядрами процесора, вся дія вмістилася в одне блискавичне завдання.

---

**Висновок: **

> *"При використанні функції `cache()` кількість Job зменшилася з 8 до 7 завдяки збереженню проміжних обчислень. 
Під час виконання першого Action (`collect()`) Spark виконав усі важкі трансформації (читання та Shuffle) і зберіг готовий DataFrame в оперативній пам'яті (RAM). 
Коли викликався другий `collect()`, Spark не став перераховувати весь граф (DAG) з нуля. 
Він просто звернувся до закешованих даних у пам'яті, щоб застосувати фінальний фільтр. 
Уникнення повторного читання з диска та перемішування даних скоротило кількість завдань для другої дії з 3 до 1, що й дало в сумі 7 Job замість 8."*
'''